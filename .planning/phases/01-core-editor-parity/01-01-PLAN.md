---
phase: 01-core-editor-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [TS-1, TS-2, M-1, M-2, M-5]
must_haves:
  truths:
    - "App opens from file:// as a single index.html with zero console errors and zero network requests"
    - "Palette shows all categorized shape groups and dragging a shape to the canvas creates a node"
    - "Nodes render with icon, label, color swatch, and 4 hover-revealed drag-ports"
    - "Zones render as dashed rounded rects with labels and contain nodes visually"
  artifacts:
    - path: "index.html"
      provides: "Single-file editor skeleton: tokens, palette, node/zone rendering"
      min_lines: 500
    - path: "index.html"
      contains: ["const TYPES", "const ICONS", "const PALETTE_GROUPS", "function renderNode", "function renderZone", "function addNode", "function addZone", "function inZone"]
  key_links:
    - from: "PALETTE_GROUPS"
      to: "buildPalette()"
      via: "iteration building .pitem DOM per group"
      pattern: "PALETTE_GROUPS"
    - from: "renderNode(n)"
      to: "ICONS + TYPES"
      via: "TYPES[n.type].shape selects card vs flow path; ICONS[type] supplies path data"
      pattern: "renderNode"
---

<objective>
Build the single-file editor skeleton from the validated prototype: full token CSS, catalogs (TYPES/ICONS/PALETTE_GROUPS/SWATCHES/ZONE_COLORS), palette with drag-to-canvas ghost, node rendering (card + flow shapes with icon/label/color/ports), and zone rendering with containment.

Purpose: This is the foundation every later plan builds on — no other plan can run until index.html exists with state, rendering, and catalogs.
Output: index.html (~700-900 lines) with working palette → node creation and zone creation.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-editor-parity/01-RESEARCH.md
@.planning/research/prototype-replica.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy prototype to index.html; add constants, catalogs, state model</name>
  <files>index.html</files>
  <read_first>
  - .planning/research/prototype-replica.html (entire file — styling skeleton; copy its CSS/DOM/script structure)
  - .planning/phases/01-core-editor-parity/01-RESEARCH.md — sections "Recommended Project Structure" + "Pattern 1" + Key Findings 1-2
  </read_first>
  <action>
  Start from the prototype: same :root token block, same #app layout (52px topbar, 212px palette, canvas, hintbar, props, labelEditor), same script section order. Then extend:
  1. Rename prototype's SHAPES catalog to TYPES with per-entry fields: cat, shape ("card"|"flow"), and icon (path data string). Port the 8 prototype shapes and add the original's remaining catalog entries so PALETTE_GROUPS' 9 groups are fully populated: Network (internet, cloud, router, switch, wifi, asn, ipaddress), Security & Edge (firewall, waf, proxy, cdn, loadbalancer, apigateway), Servers (server, webserver, appserver, mgmtserver, updateserver, backupserver, telephony, hypervisor), Storage & Data (database, san, nas), Endpoints (workstation, laptop, phone, printer, scanner, fax, cardreader, barcodereader, iot, ot), People & Misc (person, email), Places & Orgs (org, building, city, location, poi), Process Flow (startend, process, decision, dataio, document, predefined, preparation, manualinput, delay, connector), Threats (threatactor, insider, malware, ransomware, botnet, phishing, c2). Every flow-shape entry gets shape:"flow".
  2. Split ICONS out of TYPES as its own catalog (key → inline SVG path string, 24x24 viewBox, stroke-based like prototype's ic() helper consumes). Clean-room: draw simple recognizable paths, do NOT read .planning/research/original-behavior-reference.js content for this task.
  3. Constants: NW=104, NH=74, GRID=26, SNAP=13, LANE_SNAP=22, SVGNS, LS_KEY='netdraw.doc.v1'.
  4. SWATCHES (11 colors starting #38bdf8, #60a5fa, #a78bfa, #c084fc, #f472b6, #f87171, #fb923c, #fbbf24, #34d399, #2dd4bf, #94a3b8) and ZONE_COLORS (8 colors starting #38bdf8, #a78bfa, #34d399, #f59e0b, #f472b6, #2dd4bf, #f87171, #c084fc).
  5. State model per RESEARCH Pattern 1: state = {nodes:[], edges:[], zones:[]} as the single source of truth; uid counters n/e/z shared with id strings; view = {x,y,k}; tool; sel; drag/panning/spaceHeld/zoneDraw interaction variables. Keep helpers: $, el, esc, clamp, snap, nodeById/edgeById/zoneById, accentOf, nodeW, nodeH, metaLines, inZone.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('index.html','utf8'); for (const s of ['const TYPES','const ICONS','const PALETTE_GROUPS','const SWATCHES','const ZONE_COLORS','NW=104','NH=74','GRID=26','SNAP=13','LANE_SNAP=22','function inZone','function nodeW','function nodeH']) { if(!t.includes(s)) { console.error('MISSING: '+s); process.exit(1);} } console.log('ok')" && grep -c 'shape:' index.html</automated>
  </verify>
  <acceptance_criteria>
  - index.html exists; contains const TYPES, const ICONS, const PALETTE_GROUPS, const SWATCHES (11 entries), const ZONE_COLORS (8 entries)
  - Constants NW=104, NH=74, GRID=26, SNAP=13, LANE_SNAP=22 present verbatim
  - All 9 PALETTE_GROUPS titles present: Network, Security & Edge, Servers, Storage & Data, Endpoints, People & Misc, Places & Orgs, Process Flow, Threats
  - Every type key referenced in PALETTE_GROUPS exists in TYPES (node -e script checking group items against Object.keys(TYPES) passes)
  - state object literal {nodes, edges, zones} present; no separate loose arrays for the model
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: renderNode / renderZone / renderAll + palette drag ghost</name>
  <files>index.html</files>
  <read_first>
  - index.html (the file just written — Task 1's catalogs and helpers)
  - .planning/research/prototype-replica.html — render() function (lines 301-337), buildPalette (220-238), startPaletteDrag (239-256), port rendering (330-333), zone drawing (304-309)
  - .planning/phases/01-core-editor-parity/01-RESEARCH.md — Pattern 1, Key Finding 2
  </read_first>
  <action>
  1. renderNode(n): translate(n.x,n.y) group. Card path (TYPES[n.type].shape==="card"): NW x NH rect, rx 11, fill var(--card), stroke n.color (2px when selected), shadow rect offset (6,6) rgba(0,0,0,.35); icon via ICONS[n.type] colored n.color; label centered at (NW/2, NH-8) 11px 600 fill var(--text); meta lines per metaLines(n) at 10px fill var(--muted); 4 port circles at (NW/2,0),(NW/2,NH),(0,NH/2),(NW,NH/2) r 4.5, fill #0b1120, stroke n.color, data-node=n.id, wrapped in a .ports group (CSS opacity 0, g:hover .ports opacity 1 — already in prototype CSS). Flow path (shape==="flow"): label-width sizing via nodeW(n), centered text, no meta lines, no ports.
  2. renderZone(z): group .zoneg; rect x/y/w/h rx 16 fill rgba(zone-color,.05) stroke z.color stroke-dasharray "10 7" stroke-width 1.4; label pill at top-left: rect rx 8 + text 10.5px 600 uppercase-tracking fill z.color. Swimlane zones (kind==="swimlane") are NOT rendered by renderZone — leave a stub branch delegating to renderSwimlane (implemented in Plan 02).
  3. renderAll(): clear zonesLayer/edgesLayer/nodesLayer innerHTML, iterate state.zones then state.edges then state.nodes, call renderZone/renderEdge/renderNode, then updateProps(), updateToolbar(), applyView(). renderEdge may be a placeholder line (a-center to b-center) in this plan — Plan 02 replaces it with edgeGeom.
  4. buildPalette(): iterate PALETTE_GROUPS building .pgroup > h3 (uppercase 10.5px) + .pitems grid; per item render icon into .pic, name into .pname, wire pointerdown → startPaletteDrag(ev, typeKey).
  5. startPaletteDrag(ev, key): fixed-position ghost .pitem clone following pointer (pointermove on window); on pointerup, if drop point inside svg bounds: addNode(key, worldX - NW/2, worldY - NH/2), setTool("select"), commit(). Ghost always removed.
  6. addNode(type,x,y,label,color): push {id, type, x, y, label:label||type, color:color||SWATCHES[0], meta:{}} to state.nodes; select it; renderAll(). addZone(x,y,w,h,label,color): push {id,x,y,w:Math.max(w,80),h:Math.max(h,60),label,color:color||ZONE_COLORS[0],kind:"zone"}; select; renderAll(). Both call commit() (added in Plan 02; guard with typeof commit==="function" if needed — better: define minimal commit() stub that Plan 02 expands).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('index.html','utf8'); for (const s of ['function renderNode','function renderZone','function renderAll','function buildPalette','function startPaletteDrag','function addNode','function addZone','data-node']) { if(!t.includes(s)) { console.error('MISSING: '+s); process.exit(1);} } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
  - index.html contains function renderNode(, function renderZone(, function renderAll(, function buildPalette(, function startPaletteDrag(, function addNode(, function addZone(
  - renderNode renders 4 .port circles with data-node attribute; .ports group has opacity 0 in CSS with hover reveal
  - renderZone draws rect with stroke-dasharray "10 7" and rx 16
  - Palette DOM: every PALETTE_GROUPS entry produces a .pgroup with .pitem children — count via querySelectorAll('.pitem').length equals total items across groups
  - Drag-drop path: startPaletteDrag ends with addNode(...) call and setTool("select")
  - Zero external network references: grep for http:// https:// //cdn fetch( XMLHttpRequest WebSocket link href outside inline data: returns nothing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-drawn content → SVG DOM | labels/colors typed by the user are rendered into SVG text/attrs |
| prototype CSS/DOM reuse | styling copied from own clean-room prototype (trusted, own code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | renderNode/renderZone label text | medium | mitigate | All label text inserted via textContent or esc() (prototype's helper) — never innerHTML with user strings; attribute values set via setAttribute. Acceptance: label containing script-tag text renders as inert text. |
| T-01-02 | DoS | addNode/addZone (no size limits) | low | accept | Single-user local editor; element count bounded by user's own actions; original has no limit either. Revisit if import (M-17) lands unbounded. |
| T-01-03 | Repudiation | none (no audit trail in scope) | low | accept | Client-only tool, no backend, no audit requirement at ASVS L1. |
</threat_model>

## Artifacts this phase produces

(Single-file app; all symbols live in index.html.)

- File: `index.html` (the entire app)
- CSS classes: `.pgroup`, `.pitems`, `.pitem`, `.pic`, `.pname`, `.tgroup`, `.sep`, `.zoneg`, `.nodeg`, `.ports`, `.port`, `.edge`, `.vis`, `.sw`, `.swatches`
- Catalogs: `TYPES` (48 entries across 9 categories), `ICONS`, `PALETTE_GROUPS` (9 groups), `SWATCHES` (11), `ZONE_COLORS` (8)
- Constants: `NW=104`, `NH=74`, `GRID=26`, `SNAP=13`, `LANE_SNAP=22`, `LS_KEY`, `SVGNS`
- Functions: `el`, `esc`, `uid`, `clamp`, `snap`, `nodeById/edgeById/zoneById`, `accentOf`, `nodeW`, `nodeH`, `metaLines`, `inZone`, `renderNode`, `renderZone`, `renderAll`, `buildPalette`, `startPaletteDrag`, `addNode`, `addZone`
- DOM IDs: `#topbar`, `#palette`, `#canvas`, `#props`, `#labelEditor`, `#hintbar`, `#gZones`, `#gEdges`, `#gNodes` (or equivalent layer group ids)

