---
phase: 06-parity-audit-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
  - docs/demo.gif
  - .gitignore
  - package.json
autonomous: true
requirements:
  - AC-1
  - AC-5
  - TS-5
must_haves:
  truths:
    - "README.md exists with a demo GIF embed (docs/demo.gif) and a Quick Start section"
    - "docs/demo.gif is a valid GIF produced by the app's own exportGIF on a representative animated-edge diagram"
    - "git push succeeds to a GitHub remote (or gh repo create --public/--private + push)"
    - ".gitignore excludes .env, node_modules, and scratch files; no secrets staged"
    - "package.json (if present for playwright devDep) is marked private and documents the app is zero-dep single-file"
    - "Edge probes resolved: TS-5 concurrency — README notes zero runtime network; AC-5 — README Quick Start documents file:// open with zero console errors"
  artifacts:
    - path: "README.md"
      provides: "OSS-style project README with demo GIF, features, quick start, dev notes"
      contains: "Quick Start"
    - path: "docs/demo.gif"
      provides: "Demo GIF exported from the app"
      min_lines: 1
    - path: ".gitignore"
      provides: "Excludes secrets, node_modules, scratch"
      contains: "node_modules"
  key_links:
    - from: "README.md"
      to: "docs/demo.gif"
      via: "Markdown image embed ![demo](docs/demo.gif)"
      pattern: "docs/demo\\.gif"
    - from: "README.md"
      to: "AUDIT.md"
      via: "Features section links to AUDIT.md for the full parity checklist"
      pattern: "AUDIT\\.md"
---

<objective>
Produce the publishable repo surface: README.md with embedded demo GIF, the demo GIF itself (dogfooded from the app), and the git push to GitHub. This is the polish+publish half of the phase, gated on the audit (Plan 06-01) passing first.

Purpose: The repo must be presentable and self-documenting; the demo GIF proves the app visually; the push publishes it.
Output: README.md, docs/demo.gif, git push confirmation.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-parity-audit-polish/06-RESEARCH.md
@.planning/phases/06-parity-audit-polish/06-01-PLAN.md
@index.html
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate demo GIF + write README.md</name>
  <files>docs/demo.gif, README.md</files>
  <read_first>index.html, .planning/phases/06-parity-audit-polish/06-RESEARCH.md (README structure pattern), .planning/PROJECT.md (Feature Inventory + goals), AUDIT.md (from 06-01 Task 2 — feature list source)</read_first>
  <action>
Generate docs/demo.gif by dogfooding the app's own exportGIF: open index.html, load a representative diagram with an animated edge (use the Examples menu's bundled demo or build a small network topology with one animated dash-flow edge), trigger GIF export, and save the output to docs/demo.gif. If the app's export writes a download, capture it to docs/. Confirm the file begins with the GIF89a signature (reuse the byte check from audit.mjs or a quick node one-liner).

Write README.md following the RESEARCH.md "Pattern 3" structure: Title + one-liner tagline; embedded demo GIF (![NetDraw demo](docs/demo.gif)); Features section (bulleted summary of Feature Inventory categories, with a link to AUDIT.md for the full side-by-side checklist); Quick Start (open index.html directly from file:// — note zero console errors / zero network, or serve statically with any static server); Usage (tool keyboard map V/C/Z/L/H, Del, F, Ctrl+Z/Y, Ctrl+S; export PNG/SVG/GIF; record; PlantUML import/export; config ingestion); Development (run node test/run-tests.js for fixture tests; node test/audit.mjs for the parity audit; note playwright is a devDependency and the app itself is zero-dependency single-file); License (MIT); Credits (inspired by the original NetDraw by mr-r3b00t; clean-room implementation, no code copied).

Do not inline large code blocks — keep Usage as a keyboard table and short prose. The README must render the GIF on GitHub (relative path docs/demo.gif committed to the repo).
  </action>
  <verify>
    <automated>test -f README.md && test -f docs/demo.gif && grep -q 'docs/demo.gif' README.md && grep -q 'Quick Start' README.md && head -c 6 docs/demo.gif | grep -q 'GIF89a'</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists with an embedded demo GIF (docs/demo.gif) and a Quick Start section.
    - docs/demo.gif exists and begins with the GIF89a signature.
    - README Features section links to AUDIT.md.
    - README documents that the app is zero-dependency single-file (TS-1/TS-5) and that playwright is dev-only.
  </acceptance_criteria>
  <done>README.md embeds docs/demo.gif (GIF89a-verified) with Quick Start; Features links to AUDIT.md; zero-dep and dev-only-playwright documented.</done>
</task>

<task type="auto">
  <name>Task 2: Secret scan + .gitignore + git push</name>
  <files>.gitignore, package.json</files>
  <read_first>index.html, README.md (from Task 1), .planning/phases/06-parity-audit-polish/06-RESEARCH.md (publishing notes)</read_first>
  <action>
Ensure .gitignore exists and excludes: node_modules/, .env, .env.*, scratch files (G:/Scratch/* references, *.log, OS files like Thumbs.db, .DS_Store), and any local-only artifacts. If package.json was added (for the playwright devDependency), confirm it is marked private: true and the description notes the app is single-file zero-dependency; playwright must be in devDependencies, not dependencies.

Before push, scan all staged files for secrets: grep staged content for common secret patterns (private key headers, AWS keys, API tokens, password assignments). Use git diff --cached or ripgrep on the staged file list. If any secret is found, unstage it and add the pattern to .gitignore. Do not push if secrets are detected — fix and re-scan.

Publish: check if a git remote named origin exists (git remote). If not, create the GitHub repo via gh repo create D:\DexProjects\netdraw-clone --public (or --private if preferred — default public for an OSS tool per PROJECT.md goals) with a description matching the README tagline, then git remote add origin the new repo URL. Then git push -u origin HEAD (push the current branch). If gh is unavailable or not authenticated, fall back to manual instructions in the task output (create the repo in the GitHub UI, then add remote + push) — but attempt the CLI path first. Confirm the push succeeds (git log origin/HEAD or gh repo view).

This task depends on 06-01 passing (audit green) — do not push a red audit.
  </action>
  <verify>
    <automated>git remote -v | grep -q origin && git log --oneline origin/HEAD -1 && ! git diff --cached --name-only | grep -q '\.env'</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore excludes node_modules, .env, scratch files.
    - No secrets staged (secret scan clean).
    - git push succeeds (or gh repo create + push succeeds); origin remote exists and HEAD is pushed.
    - package.json (if present) is marked private with playwright in devDependencies.
  </acceptance_criteria>
  <done>Secret scan clean; .gitignore excludes .env/node_modules/scratch; push to origin succeeds with HEAD on the remote.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub remote | Public exposure of all committed files — secrets, internal paths, test fixtures become world-readable (if public) |
| demo.gif → README embed | Binary committed to repo; must be app-generated, not external |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | git push exposing secrets / internal paths | high | mitigate | Secret scan all staged files before push; .gitignore excludes .env and scratch; review for absolute local paths (D:\, G:\) in committed text — scrub or relativize |
| T-06-06 | Tampering | package.json accidentally shipping playwright as a dependency | medium | mitigate | Verify playwright is in devDependencies and package.json private:true; the app (index.html) never imports it |
| T-06-07 | Spoofing | gh repo create targeting the wrong account | low | accept | Single-user gh auth; verify repo URL after create before push |
| T-06-08 | Repudiation | README claims not backed by audit | low | mitigate | README links to AUDIT.md; AUDIT.md statuses come from audit.mjs exit code (machine-verifiable) |
</threat_model>

## Artifacts this phase produces

| File | Type | Notes |
|------|------|-------|
| README.md | new | OSS-style docs with demo GIF embed |
| docs/demo.gif | new | App-exported animated demo |
| .gitignore | new | Excludes secrets, node_modules, scratch |
| package.json | new (if playwright devDep added in 06-01) | private:true, devDependencies only |
| AUDIT.md | from 06-01 | Referenced by README (not modified here) |

<verification>
- README.md renders the demo GIF (relative path docs/demo.gif).
- docs/demo.gif has GIF89a signature.
- .gitignore present and excludes secrets/scratch.
- Secret scan clean before push.
- git push to origin succeeds.
</verification>

<success_criteria>
- README with embedded demo GIF documents the project (AC-1 docs surface).
- Repo pushed to GitHub.
- No secrets exposed.
- App remains zero-dependency single-file (TS-1/TS-5 documented in README).
</success_criteria>

<output>
Create `.planning/phases/06-parity-audit-polish/06-02-SUMMARY.md` when done.
</output>